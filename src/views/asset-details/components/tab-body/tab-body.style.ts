import {StyleSheet} from 'react-native';
import {COLORS} from '../../../../assets';

export const tabBodyStyle = StyleSheet.create({
  tabBody: {
    backgroundColor: COLORS['bg-100-dark'],
    paddingLeft: 16,
    paddingRight: 16,
    flex: 1
  },
});
