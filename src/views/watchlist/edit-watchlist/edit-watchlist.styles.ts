import {StyleSheet} from 'react-native';
import {COLORS} from '../../../assets';

export const editWatchlistStyles = StyleSheet.create({
  container: {
    backgroundColor: COLORS['bg-100-dark'],
    flex: 1
  },
});
